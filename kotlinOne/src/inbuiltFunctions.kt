fun main(args: Array<String>) {
    var number:Double
    number = 10.0

    println(Math.pow(number,2.0))

    println(5/2)
    println(5/2.0)//working with decimals gives exact results

    var x:String
    var y:String

    var a:Double
    var b:Double

    var p:Int
    var q:Int

    x = "140"
    y = "22"

    a = x.toDouble()
    b = y.toDouble()

    p = x.toInt()
    q = y.toInt()

    println(x+y)
    println(a+b)
    println(p+q)
}